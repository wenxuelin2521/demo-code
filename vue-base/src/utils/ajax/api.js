import { fetchData } from "./commonAjax.js";

// template
// export function queryList(requestParams, callFunc) {
//   callFunc = callFunc && callFunc.constructor === Function ? callFunc : ""; // 回调校验
//   return fetchData(`/api`, requestParams, "get", callFunc);
// }
